# Database Configuration Template
# Copy this content to a .env file and update with your actual database credentials

# Oracle Database Configuration (Primary)
ORACLE_HOST=localhost
ORACLE_PORT=1521
ORACLE_SERVICE=XE
ORACLE_USER=system
ORACLE_PASSWORD=oracle
ORACLE_DSN=localhost:1521/XE

# PostgreSQL Database Configuration (Fallback)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=bank_reviews
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Note: 
# - For Oracle XE, the default service name is usually 'XE'
# - For PostgreSQL, you may need to create the 'bank_reviews' database first
# - Update passwords to match your actual database setup
# - Save this as .env file in the project root directory
